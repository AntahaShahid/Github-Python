#Analyzing the length of a String:

name="Antaas"
print(len(name))

# we can expand the same code to multiple lines:

name=input("Enter Your name: ")
length=len(name)
print(f"Length of Your string is : {length}")