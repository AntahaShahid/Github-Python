# Lets calculate area of a rectangle by taking user input:

length=float(input("Enter the length of recatangle: "))
width=float(input("Enter the width of recatangle: "))

Area=length*width

print(f"The area of recatangle is {Area} cm 2")
